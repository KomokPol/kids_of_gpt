{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "wallet.credited",
  "type": "object",
  "properties": {
    "event_id": { "type": "string" },
    "event_type": { "const": "wallet.credited" },
    "aggregate_type": { "const": "wallet" },
    "aggregate_id": { "type": "string" },
    "occurred_at": { "type": "string" },
    "producer": { "type": "string" },
    "correlation_id": { "type": "string" },
    "payload": { "type": "object" }
  },
  "required": ["event_id", "event_type", "aggregate_type", "aggregate_id", "occurred_at", "producer", "correlation_id", "payload"]
}